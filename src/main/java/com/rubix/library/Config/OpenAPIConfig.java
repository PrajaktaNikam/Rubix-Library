package com.rubix.library.Config;

//////////////////////////////////////////////////////////////////
// Swagger/OpenAPI Config

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//////////////////////////////////////////////////////////////////

@Configuration
class OpenAPIConfig {
/*
    @Bean
    public OpenAPI apiInfo() {
        return new OpenAPI()
                .info(new Info()
                        .title("Library API")
                        .version("1.0")
                        .description("API documentation for library authors management"));
    }

    @Bean
    public OpenAPI libraryApiInfo() {
        return new OpenAPI()
                .info(new Info()
                        .title("Library Book API")
                        .version("1.0")
                        .description("API for managing library books"));
    }

    @Bean
    public OpenAPI issuedBookStudentApi() {
        return new OpenAPI()
                .info(new Info()
                        .title("Student Issued Book API")
                        .version("1.0")
                        .description("Endpoints for handling student book issuance"));
    }

    @Bean
    public OpenAPI issuedBookTeacherApi() {
        return new OpenAPI()
                .info(new Info()
                        .title("Teacher Issued Book API")
                        .version("1.0")
                        .description("Endpoints for handling teacher book issuance"));
    }*/
}
